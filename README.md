# Usable Design Blog Archive

Static archive generator for the legacy WordPress blog export in [`Wordpress export`](./Wordpress%20export).

## Build

```bash
npm run build
```

The generated site is written to [`site`](./site).
